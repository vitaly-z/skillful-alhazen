[project]
name = "alhazen-core"
version = "0.1.0"
description = "Core infrastructure plugin for Alhazen skills — sets up TypeDB and loads the base schema"
requires-python = ">=3.11"
dependencies = [
    "typedb-driver>=3.8.0",
]

[tool.uv]
dev-dependencies = []
