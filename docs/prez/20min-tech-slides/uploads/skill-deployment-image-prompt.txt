A technical architecture diagram. Dark navy background (#070d1c). Teal/cyan bordered boxes with small-caps labels, flat line-art icons, labeled arrows. No code. No bullet text. Icons and short labels only. 16:9 aspect ratio.

Layout: two rows.

TOP ROW — a single wide box labeled "SKILL PACKAGE" with a teal border and a subtle package/box icon in the corner.
Inside, four icon-and-label pairs arranged horizontally with equal spacing:

  [document icon]       [database schema icon]    [terminal/script icon]    [browser window icon]
    SKILL.md              SCHEMA  (.tql)              SCRIPTS                  DASHBOARD

Each icon is a clean flat line-art glyph. A dotted border wraps all four, visually grouping them as a single deployable unit.

A downward arrow from this box with the label "make build-skills" and a small gear/cog icon beside it.

BOTTOM ROW — three boxes side by side, connected by horizontal arrows:

Box 1 (teal border) — label "CLAUDE CODE"
  Large flat asterisk/spark icon (Anthropic logo style) centered in the box.
  Small label beneath: "agent interface"

Horizontal arrow pointing right → labeled "TypeQL"

Box 2 (blue border) — label "TYPEDB"
  Docker whale flat icon centered, with a small cylinder (database) icon stacked below it.
  Small label beneath: "local notebook"

Horizontal arrow pointing right → labeled "live query"

Box 3 (olive/yellow-green border) — label "DASHBOARD"
  Flat browser/monitor icon centered, with a small bar-chart icon inside the screen area.
  Small label beneath: "visual explorer"

Dashed curved arrows connect Box 1 and Box 3 back up to the SKILL PACKAGE box at the top, indicating that SKILL.md drives Claude and the dashboard component drives the UI.

Overall aesthetic: matches a dark terminal / architecture diagram. Teal borders, small-caps labels, flat icons only, clean connecting arrows. No code snippets, no bullet lists, no long text.
